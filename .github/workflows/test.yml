name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 24f2008353@ds.study.iitm.ac.in
        run: echo "Hello from GitHub Actions"

      - name: Second Step
        run: echo "Workflow executed"
